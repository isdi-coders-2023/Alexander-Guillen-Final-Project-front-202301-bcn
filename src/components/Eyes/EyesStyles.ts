import { StyleSheet } from "react-native";

const EyesStyles = StyleSheet.create({
  eyesContainer: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "flex-end",
    gap: 101,
  },
  eye: {
    width: 70,
    height: 70,
  },
});

export default EyesStyles;
