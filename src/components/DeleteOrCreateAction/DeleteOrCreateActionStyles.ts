import { StyleSheet } from "react-native";

const DeleteOrCreateActionStyles = StyleSheet.create({
  delete: {
    position: "absolute",
    zIndex: 100,
    left: "80.89%",
    top: "2%",
  },
});

export default DeleteOrCreateActionStyles;
